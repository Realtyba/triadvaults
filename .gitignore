node_modules/
.env
dist/
.DS_Store

# Salidas del empaquetado de escritorio. `build/` también se genera: el icono lo
# produce `npm run icon` a partir de scripts/make-icon.js, que sí está versionado.
release/
build/
